export default function Button(link, split, text, btnClass, ref) {

    // const mainLink = options.find(x => x.key === 'mainLink').value
    // const ampLink = `tap:AMP.navigateTo(url='${mainLink}')`
    // const handleButtonClick = function() {
    //   location.href = mainLink
    // }

    return (
      <>
      <button>text</button>
      {/* <button className={btnClass} onClick={handleButtonClick} on={ampLink}>
        {text}
      </button> */}
      </>
    )

}